name: workflow-ci

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str}
      target_col: {type: str, default: "Diabetic"}
      is_train_col: {type: str, default: "is_train"}
    command: "python modelling.py --data-path {data_path} --target-col {target_col} --is-train-col {is_train_col}"
