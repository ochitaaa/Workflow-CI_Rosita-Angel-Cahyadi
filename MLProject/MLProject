name: Credit Score Classification
version: 1.0

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path:
        type: string
        default: "Credit Score Dataset_preprocessing.csv"
    command: "python modelling.py --data_path {data_path}"
